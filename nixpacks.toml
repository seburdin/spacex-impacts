[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = [
  'npm config set cache /tmp/npm-cache --global',
  'npm ci --prefer-offline --no-audit'
]

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npm start'
